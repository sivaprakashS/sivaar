#include<stdio.h>
#include<string.h>
void main()
{
char str[10];
int i = 0, l = 0, f = 1;
puts("Enter any string");
gets(str);
for(i = 0;i<=;i++)
{
l = l + 1;
}
printf("The number of characters in the string are %d", l);
}
